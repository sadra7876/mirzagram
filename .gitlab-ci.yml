stages:
  - build
  - deploy
  
build:
  stage: build
  image: node:21.1.0-alpine

variables:
  API_IMAGE: mirzagram1234/mirzagram

api-build-job:
  stage: build
  script:
    - cd backend
    - docker build -t ${API_IMAGE}:$CI_COMMIT_SHORT_SHA .
    - docker push ${API_IMAGE}:$CI_COMMIT_SHORT_SHA

