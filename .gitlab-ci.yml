# image: node:latest
stages:
  - build
build:
  stage: build
  image: node:21.1.0-alpine
#   before_script:
#     - corepack enable
#     - corepack prepare pnpm@latest-9 --activate
#     - pnpm config set store-dir .pnpm-store
  script:
    - cd backend  
    - yarn install
    - yarn build
    # - pnpm test
#   cache:
#     key:
#       files:
#         - pnpm-lock.yaml
#     paths:
#       - .pnpm-store
